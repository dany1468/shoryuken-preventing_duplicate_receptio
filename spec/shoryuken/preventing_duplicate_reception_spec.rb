require "spec_helper"

RSpec.describe Shoryuken::PreventingDuplicateReception do
  it "has a version number" do
    expect(Shoryuken::PreventingDuplicateReception::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
